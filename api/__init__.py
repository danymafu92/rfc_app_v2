"""API Django app package.

This package contains the API app's models, views, serializers and URL
registrations. It is intentionally lightweight; models are `managed = False`
because the schema lives in an external Supabase/Postgres instance.
"""
