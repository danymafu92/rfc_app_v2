"""Migrations package for the `api` app.

This repository does not include Django-managed migrations for the API app
because the production schema is owned by Supabase/Postgres. Keep this
package present so Django recognizes the app's migrations package.
"""
