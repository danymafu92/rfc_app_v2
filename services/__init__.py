"""Helper services used by the API (Open-Meteo client, risk calculator).

This package exposes `open_meteo.OpenMeteoService` and
`risk_calculator.RiskCalculator` which are used throughout `api.views`.
"""
